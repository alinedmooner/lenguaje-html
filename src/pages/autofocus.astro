---
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Atributo autofocus</title>
  </head>
  <body>
    <h1>autofocus</h1>
    <p>El atributo autofocus es un atributo booleano en HTML que se utiliza para indicar que un elemento debe recibir automáticamente el enfoque cuando se carga la página o se muestra un cuadro de diálogo. Sin embargo, su uso excesivo o inapropiado puede ser confuso o problemático para personas con discapacidades visuales que utilizan tecnología de lectura de pantalla, así como para personas con dificultades cognitivas.</p>
    <p>Al asignar el enfoque automáticamente a un elemento, los lectores de pantalla pueden "teletransportar" a los usuarios directamente a ese elemento, sin darles aviso previo ni contexto sobre el resto de la página. Esto puede ser desorientador y dificultar la comprensión del contenido para algunas personas.</p>
    <p>Es importante tener en cuenta la accesibilidad al utilizar el atributo autofocus. Se recomienda utilizarlo de manera cuidadosa y considerar alternativas o ajustes que brinden una experiencia más inclusiva. Además, es esencial realizar pruebas de accesibilidad para asegurarse de que el enfoque automático no cause dificultades o barreras para los usuarios con discapacidades.</p>
  </body>
</html>
<style>
	@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700;900&display=swap');

	:root {
		--background: #001f3f;
		--text: #f8f9fa;
		--heading: #ffd700;
		--hover: #FF731D;
		--foreground: #3B185F;
		--foreground-2: #800020;
		--foreground-3: #03001C;
	}

	h1 {
		color: var(--heading);
		font-family: 'Roboto', sans-serif;
		font-weight: 900;
		text-align: center;
	}

	p {
		color: var(--text);
		font-family: 'Roboto', sans-serif;
	}

	html {
		--s: 30px; /* control the size */
		--c1: var(--background);
		--c2: var(--foreground-3);
		
		--_s:37.5% 12.5% at 62.5%;
		--_g:34% 99%,#0000 101%;
		--g1:radial-gradient(var(--_s) 100%,#0000 32%,var(--c1) var(--_g));
		--g2:radial-gradient(var(--_s) 0   ,#0000 32%,var(--c1) var(--_g));
		--g3:radial-gradient(var(--_s) 100%,#0000 32%,var(--c2) var(--_g));
		--g4:radial-gradient(var(--_s) 0   ,#0000 32%,var(--c2) var(--_g));
		background:
			var(--g1),
			var(--g2) 0                calc(3*var(--s)),
			var(--g3) var(--s)         calc(3*var(--s)),
			var(--g4) var(--s)         calc(6*var(--s)),
			var(--g1) calc(2*var(--s)) calc(6*var(--s)),
			var(--g2) calc(2*var(--s)) calc(9*var(--s)),
			var(--g3) calc(3*var(--s)) calc(9*var(--s)),
			var(--g4) calc(3*var(--s)) 0,
			repeating-linear-gradient(var(--c1) 0 25%,var(--c2) 0 50%);
		background-size: calc(4*var(--s)) calc(12*var(--s));
		display: flex;
		justify-content: center;
		align-items: center;
		flex-direction: column;
	}
</style>